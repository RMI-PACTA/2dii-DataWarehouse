

def run_migrations(
    db_connection,
    migrations_path="./sql"
):
    "Run all migrations that haven't already been run against the database"
    pass


def get_dw_version(db_connection):
    """Query the database to determine which migrations have already been run
    against it."""
    pass
